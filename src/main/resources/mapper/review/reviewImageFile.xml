<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.travelcompass.review.model.mapper.ReviewImageFileMapper">

	<resultMap type="reviewImageFileDto" id="image">
		<result column="review_image_file_id" property="reviewImageFileId"/>
		<result column="trip_review_id" property="tripReviewId"/>
		<result column="path" property="path"/>
	</resultMap>
	
	<insert id="addImageByTripReviewId" parameterType="map">
		INSERT INTO review_image_file (trip_review_id, path)
		VALUES(#{tripReviewId}, #{path})
	</insert>
	
	<select id="findByTripReviewId" parameterType="int" resultMap="image">
		SELECT *
		FROM review_image_file 
		WHERE trip_review_id = #{tripReviewId}
	</select>
	
</mapper>